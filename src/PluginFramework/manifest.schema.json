{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Project: Laminar plugin manifest schema",
  "description": "The schema required for the manifests of Project: Laminar plugins",
  "type": "object",
  "properties": {
    "file-name": { 
      "type": "string",
      "title": "The name of the plugin file",
      "description": "The version number will be appended to this to make the package file",
      "pattern": "^[\\w\\s.\\-]+$"
    },
    "version": {
      "type": "number",
      "title": "The version of the plugin",
      "description": "Appended to 'file-name' to make the name of the package file"
    },
    "user-friendly-name": {
      "type": "string",
      "title": "The name of the plugin shown to the user"
    },
    "description": {
      "type": "string",
      "title": "A user-friendly description of the plugin"
    },
    "supported_platforms": {
      "type": "array",
      "items": {
        "enum": [
          "Windows",
          "MacOS",
          "Linux"
        ]
      }
    },
    "frontend_dependencies": {
      "type": "array",
      "items": {
        "enum": [
          "Avalonia"
        ]
      }
    }
  },
  "required": ["file-name", "version"]
}